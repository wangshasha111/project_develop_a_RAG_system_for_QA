# 最小依赖 - 仅用于测试调试模式
# Minimal dependencies - For testing debug mode only

# 核心Web框架
streamlit>=1.32.0
python-dotenv>=1.0.0
pillow>=10.0.0

# 基础工具
numpy>=1.24.0
requests>=2.31.0

pypdf
# 注意：此配置仅支持调试模式
# Note: This setup only supports debug mode
# 要使用完整功能，请安装 requirements.txt
# For full functionality, install requirements.txt
